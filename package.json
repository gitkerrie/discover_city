{
  "name": "discover-china-food",
  "version": "2.0.0",
  "description": "寻味中国 - 中国美食探索地图",
  "main": "index.html",
  "scripts": {
    "prestart": "node scripts/prepare-map-assets.js",
    "start": "node scripts/server.js",
    "predev": "node scripts/prepare-map-assets.js",
    "dev": "node scripts/server.js",
    "build": "node scripts/prepare-map-assets.js && node scripts/generate-site.js && node scripts/build-output.js",
    "generate": "node scripts/prepare-map-assets.js && node scripts/generate-site.js",
    "prepare:images": "node scripts/prepare-dish-images.js",
    "export:cards": "node scripts/export-marketing-cards.js",
    "verify:map": "node scripts/verify-map.js",
    "check": "node --check js/data.js && node --check js/app.js && node --check js/content-page.js && node --check scripts/server.js && node --check scripts/prepare-map-assets.js && node --check scripts/prepare-dish-images.js && node --check scripts/generate-site.js && node --check scripts/build-output.js && node --check scripts/export-marketing-cards.js && node --check scripts/verify-map.js && node scripts/prepare-map-assets.js && node scripts/validate-data.js && node scripts/generate-site.js --check && node scripts/test-app.js && node scripts/test-site.js && node scripts/build-output.js"
  },
  "keywords": [
    "food",
    "map",
    "exploration",
    "china",
    "cuisine"
  ],
  "author": "Discover China Food",
  "license": "MIT",
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^6.5.2",
    "@maplibre/maplibre-gl-leaflet": "^0.1.3",
    "leaflet": "^1.9.4",
    "maplibre-gl": "^5.24.0",
    "playwright": "^1.61.0",
    "sharp": "^0.34.5",
    "topojson-client": "^3.1.0",
    "world-atlas": "^2.0.2"
  }
}
